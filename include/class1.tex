\element{class1}{
  \begin{question}{c1q1}
    What role does the private key play in a digital signature using RSA in blockchain?
    \begin{choices}
      \correctchoice{Verifying the signature}
      \wrongchoice{Encrypting the message}
      \wrongchoice{Decrypting the message}
      \wrongchoice{Generating the hash}
    \end{choices}
  \end{question}
}

\element{class1}{
  \begin{question}{c1q2}
    In a blockchain network, how does the verification process work for a digital signature?
    \begin{choices}
      \wrongchoice{Decrypting the signature using the public key}
      \wrongchoice{Encrypting the signature using the private key}
      \correctchoice{Matching the hash with the public key}
      \wrongchoice{Matching the hash with the private key}
    \end{choices}
  \end{question}
}
 
\element{class1}{
  \begin{question}{c1q3}
    What does "hiding input in hashing" refer to in the context of hash functions for blockchain?
    \begin{choices}
      \wrongchoice{Ensuring the hash output is unpredictable}
      \wrongchoice{Encrypting the input data}
      \correctchoice{Making the input difficult to reverse engineer}
      \wrongchoice{Preventing collisions between hash outputs}
    \end{choices}
  \end{question}
}
