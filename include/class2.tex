\element{class2}{
  \begin{question}{c2q1}
    Why is puzzle friendliness considered a desired property for hash functions in blockchain?
    \begin{choices}
      \wrongchoice{To make the hash function simple}
      \wrongchoice{To make it easy to decrypt the hash}
      \correctchoice{To create challenges for miners in the proof-of-work consensus.}
      \wrongchoice{To reduce the size of the hash output}
    \end{choices}
  \end{question}
}

\element{class2}{
  \begin{question}{c2q2}
    Which property of a hash function ensures that it is computationally infeasible to find two different inputs that produce the same hash output?
    \begin{choices}
      \wrongchoice{Puzzle friendliness}
      \wrongchoice{Hiding input in hashing}
      \correctchoice{Collision resistance}
      \wrongchoice{Deterministic output}
    \end{choices}
  \end{question}
}

\element{class2}{
  \begin{question}{c2q3}
    What is time complexity of merkle trees to verify the membership of a particular block of transaction data in original data?
    \begin{choices}
      \wrongchoice{O(n)}
      \correctchoice{O(log(n))}
      \wrongchoice{O(n*log(n))}
      \wrongchoice{O(1)}
    \end{choices}
  \end{question}
}
